import { Card } from '@components'
import { IndustryWpType } from '@partials/common'

export const AddIndustryType = () => {
    return (
        <div>
            <Card shadowType="profile">
                <IndustryWpType />
            </Card>
        </div>
    )
}
