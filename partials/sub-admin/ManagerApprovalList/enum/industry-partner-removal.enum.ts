export enum IndustryRequestRemovalStatus {
    PENDING = 'pending',
    APPROVED = 'approved',
    REJECTED = 'rejected',
}

export enum IndustryRequestsActions {
    Partner = 'Partner',
    Snoozed = 'Snoozed',
    NonContactable = 'nonContactable',
    Flagged = 'flagged',
    UnFlagged = 'unFlagged',
}
