import { Notes } from '@components/sections/subAdmin/components'
import React from 'react'

export const AllNotes = ({ id }: any) => {
    return (
        <div>
            <Notes id={id} />
        </div>
    )
}
